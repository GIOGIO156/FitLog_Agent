# Repeated

Use [OpenAI](https://developers.openai.com/api/docs) and email rag@example.com.
Keep `gram_per_kg`, `per_side_reps`, `CHANGELOG.md`, `app_database.dart`, `index.ts`, and `schema.sql` exact.
Versions are 0.85, v1.2.3, and 2026-07-13. Reference RAG-S04.

| field | value |
| --- | --- |
| enum | `per_side_reps` |

```json
{"mode":"gram_per_kg","path":"docs/en/RAGDesign.md"}
```

# Repeated

中文标点不能破坏：括号（每侧次数）；链接和路径保持原样。

```sql
select * from public.document_chunks;
```

```dart
const source = 'CloudLocalDataBoundary.md';
```
